---
driver:
  name: proxy
  host: localhost
  reset_command: echo hello
  port: <%= ENV["MACHINE_PORT"] %>
  username: <%= ENV["MACHINE_USER"] %>
  password: <%= ENV["MACHINE_PASS"] %>

platforms:
    - name: ubuntu-latest
    - name: macos-latest
    - name: windows-latest
#       transport:
#         name: winrm
#         elevated: true
