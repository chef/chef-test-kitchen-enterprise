---
# Minimal config used only for Habitat package smoke tests.
# Intentionally avoids external infrastructure.

driver:
  name: dummy

transport:
  name: dummy

provisioner:
  name: dummy

verifier:
  name: dummy

platforms:
  - name: localhost

suites:
  - name: default
